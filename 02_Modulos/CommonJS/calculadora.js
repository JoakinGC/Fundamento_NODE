const sumar = (a,b) => a + b,
restar = (a,b) => a-b,
dividir = (a,b) => a / b,
multiplicar = (a,b) => a * b,
modulo = (a,b) => a % b,
calculador = {
    sumar,
    restar,
    dividir,
    multiplicar,
    modulo,
};


module.exports = calculador;




